import { PageShell } from '../../_components/shared/page-shell';

export default function GamesPage() {
    return (
        <PageShell
            title='Games'
            description='Space for game projects, prototypes, dev logs, and anything interactive you want to show off.'
        />
    );
}

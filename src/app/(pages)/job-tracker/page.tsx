import { PageShell } from '../../_components/shared/page-shell';

export default function JobTrackerPage() {
    return (
        <PageShell
            title='Job Tracker'
            description='Home for application tracking, interview progress, notes, and reminders tailored to your job search.'
        />
    );
}

import { PageShell } from '../../_components/shared/page-shell';

export default function LeetCodePage() {
    return (
        <PageShell
            title='LeetCode Stats'
            description='Future home for solved problem counts, streaks, topic breakdowns, and progress snapshots.'
        />
    );
}

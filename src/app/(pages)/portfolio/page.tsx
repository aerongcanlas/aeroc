import { PageShell } from '../../_components/shared/page-shell';

export default function PortfolioPage() {
    return (
        <PageShell
            title='Portfolio'
            description='Collection page for finished projects, case studies, screenshots, and links to source code or demos.'
        />
    );
}

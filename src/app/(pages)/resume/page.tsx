import { PageShell } from '../../_components/shared/page-shell';

export default function ResumePage() {
    return (
        <PageShell
            title='Resume'
            description='Dedicated page for your resume, experience summary, and downloadable versions when you are ready.'
        />
    );
}
